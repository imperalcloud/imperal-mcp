{
  "$schema": "https://static.modelcontextprotocol.io/schemas/2025-12-11/server.schema.json",
  "name": "io.github.imperalcloud/imperal-mcp",
  "title": "Imperal MCP",
  "description": "Build and deploy Imperal apps from any LLM. Validate, smoke-test, and deploy declarative IR apps into your Imperal account — then operate them with read-only tools. Browser OAuth, no token pasting.",
  "version": "0.2.2",
  "packages": [
    {
      "registryType": "pypi",
      "identifier": "imperal-mcp",
      "version": "0.2.2",
      "runtimeHint": "uvx",
      "transport": {
        "type": "stdio"
      },
      "environmentVariables": [
        {
          "name": "IMPERAL_API_URL",
          "description": "Override the Imperal API base URL (default: https://auth.imperal.io). Leave unset for production.",
          "isRequired": false,
          "isSecret": false
        },
        {
          "name": "IMPERAL_TOKEN",
          "description": "Optional service token for CI or headless environments. When set, skips browser login. Obtain from panel.imperal.io → Developer → Access tokens.",
          "isRequired": false,
          "isSecret": true
        }
      ]
    }
  ]
}
